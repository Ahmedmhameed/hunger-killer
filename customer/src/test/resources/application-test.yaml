spring:
  profiles:
    active: test

  datasource:
    url: jdbc:mysql://localhost:3306/customer_db_test?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8081

eureka:
  client:
    enabled: false

app:
  jwt:
    secret: test-secret-key-must-be-at-least-256-bits-long-for-testing-purposes-1234567890
    expiration: 86400000
